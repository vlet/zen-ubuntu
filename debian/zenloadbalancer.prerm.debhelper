# Automatically added by dh_installinit
if [ -x "/etc/init.d/zenloadbalancer" ] || [ -e "/etc/init/zenloadbalancer.conf" ]; then
	invoke-rc.d zenloadbalancer stop || exit $?
fi
# End automatically added section
